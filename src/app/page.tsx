import { RootLayout } from "@/common/layouts";
import Home from "@/modules/home/Home";

const HomePage = () => {
  return (
    <RootLayout>
      <Home />
    </RootLayout>
  );
};

export default HomePage;
