"use client";
import { Button } from "@/ui-library/buttons";
const Profile = () => {
  return (
    <div>
      Profile
      <Button theme="default">Exit</Button>
    </div>
  );
};

export default Profile;
